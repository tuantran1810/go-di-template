packages:
    github.com/tuantran1810/go-di-template/internal/usecases:
        config:
            dir: "mocks/usecases"
            filename: "mock_{{.InterfaceName}}.go"
        interfaces:
            IUserStore:
                config:
            IUserAttributeStore:
                config:
            IUUIDGenerator:
                config:
            IMessageStore:
                config:
            IClient:
                config:
    github.com/tuantran1810/go-di-template/internal/controllers:
        config:
            dir: "mocks/controllers"
            filename: "mock_{{.InterfaceName}}.go"
        interfaces:
            IUserUsecase:
                config:
            ILoggingWorker:
                config:
